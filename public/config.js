// Runtime configuration for production
// This file is loaded at runtime (not at build time) so you can update it without rebuilding
window.APP_CONFIG = window.APP_CONFIG || {
  // Set this to your backend URL if it's different from the frontend
  // Example: 'https://your-backend-service.onrender.com'
  BACKEND_URL: '',
  SOCKET_URL: ''
};
